## Status

- **POIs:** YAML-based Planetiler schema (`features/pois/schema.yml`) — working
- **Routes:** Java profile (`features/scripts/HikingRouteOverlay.java`) — working, replaces broken YAML approach
  - 788k features, 3,745 tiles (z8–14)
  - All network levels: iwn, nwn, rwn, lwn
  - Full stack: build → server → style verified
